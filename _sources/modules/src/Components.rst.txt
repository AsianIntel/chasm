.. default-domain:: chpl

.. module:: Components

Components
==========
**Usage**

.. code-block:: chapel

   use Components;


or

.. code-block:: chapel

   import Components;

.. class:: TendencyComponent

   .. attribute:: var input_properties: map(string, shared Properties)

   .. attribute:: var tendency_properties: map(string, shared Properties)

   .. attribute:: var diagnostic_properties: map(string, shared Properties)

   .. method:: proc init(input_properties: map(string, shared Properties), tendency_properties: map(string, shared Properties), diagnostic_properties: map(string, shared Properties))

.. class:: DiagnosticComponent

   .. attribute:: var input_properties: map(string, shared Properties)

   .. attribute:: var diagnostic_properties: map(string, shared Properties)

   .. method:: proc init(input_properties: map(string, shared Properties), diagnostic_properties: map(string, shared Properties))

